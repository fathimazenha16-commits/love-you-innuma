<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Magic Portal | Adventure Awaits</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700;900&family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-color: #0a0a0c;
            --neon-pink: #ff00ff;
            --neon-purple: #bc13fe;
            --neon-blue: #00d4ff;
            --glass: rgba(255, 255, 255, 0.1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: var(--bg-color);
            color: white;
            font-family: 'Open Sans', sans-serif;
            overflow-x: hidden;
            scroll-behavior: smooth;
        }

        h1, h2, h3 {
            font-family: 'Montserrat', sans-serif;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        /* --- Star Particle Canvas --- */
        #star-canvas {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }

        /* --- Hero Section --- */
        .hero {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 20px;
        }

        .portal-ring {
            width: 300px;
            height: 300px;
            border-radius: 50%;
            border: 8px solid var(--neon-purple);
            box-shadow: 0 0 50px var(--neon-purple), inset 0 0 50px var(--neon-pink);
            display: flex;
            justify-content: center;
            align-items: center;
            animation: pulse 3s infinite alternate;
            margin-bottom: 30px;
        }

        @keyframes pulse {
            from { transform: scale(1); filter: hue-rotate(0deg); }
            to { transform: scale(1.05); filter: hue-rotate(45deg); }
        }

        .hero h1 {
            font-size: 4rem;
            text-shadow: 0 0 20px var(--neon-pink);
            margin-bottom: 10px;
        }

        .hero p {
            font-size: 1.2rem;
            color: var(--neon-blue);
            font-weight: bold;
        }

        /* --- Adventure Map Layout --- */
        .map-container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 100px 20px;
            position: relative;
        }

        /* The Dotted Path */
        .map-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 50%;
            width: 4px;
            height: 100%;
            border-left: 4px dashed var(--neon-purple);
            z-index: -1;
            opacity: 0.3;
        }

        .section-card {
            background: var(--glass);
            backdrop-filter: blur(10px);
            border: 2px solid var(--neon-purple);
            border-radius: 20px;
            padding: 40px;
            margin: 50px 0;
            width: 80%;
            transition: all 0.4s ease;
            position: relative;
        }

        .section-card:nth-child(even) { align-self: flex-end; margin-left: auto; border-color: var(--neon-pink); }
        .section-card:hover {
            transform: translateY(-10px) scale(1.02);
            box-shadow: 0 0 30px var(--neon-purple);
            background: rgba(255, 255, 255, 0.15);
        }

        .neon-btn {
            background: transparent;
            border: 2px solid var(--neon-blue);
            color: var(--neon-blue);
            padding: 12px 25px;
            font-family: 'Montserrat', sans-serif;
            font-weight: 900;
            text-transform: uppercase;
            cursor: pointer;
            border-radius: 50px;
            transition: 0.3s;
            margin-top: 20px;
        }

        .neon-btn:hover {
            background: var(--neon-blue);
            color: black;
            box-shadow: 0 0 20px var(--neon-blue);
        }

        /* --- Hall of Fame --- */
        .achievement-list {
            list-style: none;
            margin-top: 20px;
        }
        .achievement-list li {
            margin: 10px 0;
            font-size: 1.1rem;
        }
        .achievement-list li::before {
            content: '⭐';
            margin-right: 10px;
        }

        /* --- Secret Library --- */
        .book-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }
        .book-item {
            background: rgba(0,0,0,0.5);
            padding: 15px;
            border-radius: 10px;
            text-align: center;
            border-bottom: 3px solid var(--neon-pink);
        }

        /* --- Affirmation Pop-up --- */
        .modal-overlay {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0,0,0,0.85);
            display: none;
            justify-content: center;
            align-items: center;
            z-index: 1000;
        }

        .modal-content {
            background: var(--bg-color);
            padding: 50px;
            border-radius: 30px;
            border: 4px solid var(--neon-pink);
            text-align: center;
            box-shadow: 0 0 50px var(--neon-pink);
            max-width: 400px;
        }

        .modal-content h2 { margin-bottom: 20px; color: var(--neon-pink); }
        .modal-content p { font-size: 1.5rem; margin-bottom: 30px; line-height: 1.4; }

        /* --- Responsive --- */
        @media (max-width: 768px) {
            .hero h1 { font-size: 2.5rem; }
            .section-card { width: 100%; }
        }
    </style>
</head>
<body>

    <canvas id="star-canvas"></canvas>

    <!-- Modal Pop-up -->
    <div class="modal-overlay" id="modal">
        <div class="modal-content">
            <h2>✨ Daily Magic ✨</h2>
            <p id="affirmation-text">You are braver than you believe and stronger than you seem!</p>
            <button class="neon-btn" onclick="closeModal()">I've got this!</button>
        </div>
    </div>

    <section class="hero">
        <div class="portal-ring">
            <h3 style="color: white; text-shadow: 0 0 10px white;">START</h3>
        </div>
        <h1>THE MAGIC PORTAL</h1>
        <p>A Personal Adventure Built Just For You</p>
        <button class="neon-btn" style="margin-top: 40px;" onclick="document.getElementById('map').scrollIntoView()">Enter the Realm</button>
    </section>

    <div class="map-container" id="map">
        
        <!-- Hall of Fame -->
        <div class="section-card">
            <h2>🏆 The Hall of Fame</h2>
            <p>Celebrating all the times you were absolutely legendary.</p>
            <ul class="achievement-list">
                <li>hayzi favourite </li>
                <li>Master of the dance </li>
                <li>Being the coolest niece in the multiverse</li>
            </ul>
            <button class="neon-btn">Add New Trophy</button>
        </div>

        <!-- Secret Library -->
        <div class="section-card">
            <h2>📚 The Secret Library</h2>
            <p>Encoded transmissions from your favorite Auntie/Uncle. These are "Must-Reads"!</p>
            <div class="book-grid">
                <div class="book-item">
                    <h4>Harry Potter</h4>
                    <small>Movie Night?</small>
                </div>
                <div class="book-item">
                    <h4>Percy Jackson</h4>
                    <small>Epic Quests</small>
                </div>
                <div class="book-item">
                    <h4>Spider-Verse</h4>
                    <small>Best Animation</small>
                </div>
            </div>
            <button class="neon-btn">Request a Suggestion</button>
        </div>

        <!-- Daily Magic Button -->
        <div class="section-card" style="text-align: center;">
            <h2>🔮 Crystal Ball</h2>
            <p>Need a boost of mana? Click the ball to reveal your daily power.</p>
            <button class="neon-btn" onclick="showAffirmation()">Reveal My Power</button>
        </div>

    </div>

    <footer style="text-align: center; padding: 50px; opacity: 0.6;">
        <p>Created with ✨ and Neon by your Favorite Human</p>
    </footer>

    <script>
        // --- Star Particle System ---
        const canvas = document.getElementById('star-canvas');
        const ctx = canvas.getContext('2d');
        let stars = [];

        function initCanvas() {
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
        }

        class Star {
            constructor() {
                this.x = Math.random() * canvas.width;
                this.y = Math.random() * canvas.height;
                this.size = Math.random() * 2;
                this.speedX = (Math.random() - 0.5) * 0.5;
                this.speedY = (Math.random() - 0.5) * 0.5;
                this.opacity = Math.random();
            }
            update() {
                this.x += this.speedX;
                this.y += this.speedY;
                if (this.x > canvas.width) this.x = 0;
                if (this.x < 0) this.x = canvas.width;
                if (this.y > canvas.height) this.y = 0;
                if (this.y < 0) this.y = canvas.height;
            }
            draw() {
                ctx.fillStyle = `rgba(255, 255, 255, ${this.opacity})`;
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
                ctx.fill();
            }
        }

        function createStars() {
            stars = [];
            for (let i = 0; i < 150; i++) {
                stars.push(new Star());
            }
        }

        function animate() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            stars.forEach(star => {
                star.update();
                star.draw();
            });
            requestAnimationFrame(animate);
        }

        window.addEventListener('resize', () => {
            initCanvas();
            createStars();
        });

        // --- Affirmation Logic ---
        const affirmations = [
            "You are a coding wizard in the making!",
            "Your kindness is your greatest superpower.",
            "Today, you are going to do something amazing.",
            "The universe is lucky to have your energy!",
            "You are smarter than a supercomputer!",
            "You have the heart of a hero!"
        ];

        function showAffirmation() {
            const text = affirmations[Math.floor(Math.random() * affirmations.length)];
            document.getElementById('affirmation-text').innerText = text;
            document.getElementById('modal').style.display = 'flex';
        }

        function closeModal() {
            document.getElementById('modal').style.display = 'none';
        }

        // --- Initialize ---
        initCanvas();
        createStars();
        animate();

        // Show affirmation on first load after 2 seconds
        setTimeout(showAffirmation, 2000);
    </script>
</body>
</html>